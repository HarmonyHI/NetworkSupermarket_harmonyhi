String sql1 = "CALL `cart-使用购物车创建订单`(`uid` int, addr VARCHAR(255), PHONE BIGINT)";
String sql2 = "CALL `cart-修改购物车某商品的数量`(`uid` bigint,`gid` bigint, `num` int)";
String sql3 = "CALL `cart-删除购物车的某个商品`(`uid` bigint,`gid` bigint) ";
String sql4 = "CALL `cart-直接删除某个用户购物车中所有商品`(`uid` bigint)";
String sql5 = "CALL `comments_media_url_用户向评论中添加一个图片或视频的url`(purchase_id BIGINT, url VARCHAR(255))";
String sql6 = "CALL `goods&goods_type&cart-向购物车中添加不含型号商品`(uid BIGINT,gid BIGINT,nums INT,disc DECIMAL(4,2))";
String sql7 = "CALL `goods&goods_type&cart-向购物车中添加含型号商品`(uid BIGINT,tid BIGINT,gid BIGINT,nums INT,disc DECIMAL(4,2))";
String sql8 = "CALL `goods&goods_type-商家为某商品添加属于某商品的型号`(gid BIGINT, tname VARCHAR(255), url VARCHAR(255))";
String sql9 = "CALL `goods-商家上架一款商品`(vend BIGINT, nam VARCHAR(255), descri VARCHAR(255), pr DECIMAL(10,2), danwei VARCHAR(255), amt DECIMAL(10,2))";
String sql10 = "CALL `goods-商家修改商品信息`(nam VARCHAR(255), descri VARCHAR(255), pr DECIMAL(10,2), danwei VARCHAR(255), amt DECIMAL(10,2))";
String sql11 = "CALL `goods-商家删除某个商品`(gid BIGINT)";
String sql12 = "CALL `goods_media_url-商家向某个商品中添加图片视频URL`(gid BIGINT, url varchar(255))";
String sql13 = "CALL `order-使用一个含型号商品直接创建某用户的订单`(TYPEID BIGINT, GOODSID BIGINT, `uid` int, addr VARCHAR(255), PHONE BIGINT)";
String sql14 = "CALL `order-使用一个无型号商品直接创建某用户的订单`(GOODSID BIGINT, `uid` int, addr VARCHAR(255), PHONE BIGINT, discount DECIMAL(10,2))";
String sql15 = "CALL `order-商家于用户商品状态操作`(pid BIGINT, goalstatus INT)";
String sql16 = "CALL `order-商家填写物流单号`(pid BIGINT, express VARCHAR(255))";
String sql17 = "CALL `order-用户于订单状态操作`(oid BIGINT, goalstatus INT)";
String sql18 = "CALL `order-用户修改订单收货地址`(orderid BIGINT, addr VARCHAR(255))";
String sql19 = "CALL `order-用户修改订单电话号码`(orderid BIGINT, phone BIGINT)";
String sql20 = "CALL `order-用户填写退货单号`(pid BIGINT, express VARCHAR(255))";
String sql21 = "CALL `purchase-用户上传一条某商品评论`(purid BIGINT, commentstr VARCHAR(255))";
String sql22 = "CALL `purchase-用户于单商品状态操作`(pid BIGINT, goalstatus INT)";
String sql23 = "CALL `purchase-用户对一条某商品评分`(purid BIGINT, score DECIMAL(10,2))";
String sql24 = "CALL `users-修改一个用户的信息`(username VARCHAR(255), phone BIGINT, sex INT, bthdy DATE, nick VARCHAR(255), pwd VARCHAR(255), icon VARCHAR(255))";
String sql25 = "CALL `users-创建一个新用户`(phone BIGINT, pwd VARCHAR(255))";
String sql26 = "CALL `users-注销一个用户`(userid BIGINT)";
String sql27 = "CALL `vendor-修改商家信息`(id BIGINT, pwd BIGINT, nam VARCHAR(255), addr VARCHAR(255), phone BIGINT, icon VARCHAR(255))";
String sql28 = "CALL `vendor-创建商家`(pwd BIGINT, nam VARCHAR(255), addr VARCHAR(255), phone BIGINT, icon VARCHAR(255))";
String sql29 = "CALL `vendor-注销商家`(id BIGINT)";
String sql30 = "CALL `按ID查询商品`(gid BIGINT)";
String sql31 = "CALL `按ID查询用户`(uid BIGINT)";
String sql32 = "CALL `按名称查询用户`(ipt VARCHAR(255))";
String sql33 = "CALL `按商品名称查询商品库里商品的描述与首张图`(ipt VARCHAR(255), paixv_condition VARCHAR(255), paixv_method VARCHAR(255))";
String sql34 = "CALL `查询所有的商品`()";
String sql35 = "CALL `查询所有的用户`()";
String sql36 = "CALL `查询某个商家所有商品的描述与首张图`(vid BIGINT, paixv_condition VARCHAR(255), paixv_method VARCHAR(255))";
String sql37 = "CALL `查询某商品所有型号详细信息`(gid BIGINT)";
String sql38 = "CALL `查询某商品的图片视频`(gid BIGINT)";
String sql39 = "CALL `查询某商品的所有文字信息`(pid BIGINT)";
String sql40 = "CALL `查询某商品的所有文字评论`(gid BIGINT)";
String sql41 = "CALL `查询某商家特定状态的交易列表`(vid BIGINT, stat INT)";
String sql42 = "CALL `查询某用户特定状态的订单`(uid BIGINT, stat INT)";
String sql43 = "CALL `查询某用户的信息`(uid BIGINT)";
String sql44 = "CALL `查询某用户的购物车内容`(uid BIGINT)";
String sql45 = "CALL `查询某订单的详细信息`(oid BIGINT)";
String sql46 = "CALL `查询某评论的图片视频`(pid BIGINT)";
String sql47 = "CALL `随机推送某数量商品`(num INT)";
String sql48 = "SELECT `BOOL检查商家账号密码（可用手机号做账号）`(id BIGINT, pwd VARCHAR(255))";
String sql49 = "SELECT `BOOL检查用户账号密码（可用手机号做账号）`(id BIGINT, pwd VARCHAR(255))";
String sql50 = "CALL `users-检查用户账号密码（可用手机号做账号）`(uid VARCHAR(255),pwd VARCHAR(255))";
String sql51 = "CALL `vendor-检查商家账号密码（可用手机号做账号）`(uid VARCHAR(255),pwd VARCHAR(255))";